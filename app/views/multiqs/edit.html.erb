
<H1>
  Survey Builder
</H1>
<hr class="bold">
<ol class="survey-builder">
  <li class="">
    <span>Build Survey</span>
  </li>
    <span class="bar">|</span>
  <li class="">
    <span class="page-inactive">Add Questions</span>
  </li>
</ol>
<p>
  Next, fill in the questions.
</p>
<div class="q-box-wrapper">
  <label class="big-fields-labels">
    Title:
  </label>
  <p>
    <%= @survey.title %>
  </p>

  <label class="big-fields-labels">
    Description:
  </label>
  <p>
    <%= @survey.description %>
  </p>
</div>

<hr class="bold">
<H3>
  Add a multiple Choice of Question:
</H3>
<ol class="survey-builder">
  <li>
    <span class="inactive">Select Options</span>
  </li>
    <span class="bar">|</span>
  <li>
    <span>Add Question Text</span>
  </li>
</ol>
<p>
  Next, add the text for each of your options.
</p>

<div class = "q-box-wrapper">
  <%= form_for( @multiq, :url => { :controller => "multiqs", :action => "update" }, html: { :class => "" } ) do |question_fields| %>
    <div class="form-group">
      <%= question_fields.label :question, "What is your question:", class: "big-fields-labels" %>
      <%= question_fields.text_field :question, class: "form-control", :placeholder => "eg. 'Who does number two work for?' " %>
    </div>
    <%= question_fields.fields_for :choices do |choose_answer| %>
      <div class="form-group">
        <%= choose_answer.label :name, "Option #{1 + choose_answer.index}:", class: "big-fields-labels" %>
        <%= choose_answer.text_field :name, class: "form-control", :placeholder => "eg. 'Dr Evil' " %>
      </div>
    <% end %>
    <div class="big-button-wrapper">
      <%= question_fields.submit "Add Question", class: "btn btn-primary btn-group btn-group-lg" %>
    </div>
  <% end %>
</div>
<%= link_to "Back to previous step", "#" %>
<hr class="bold">

<div class="q-box-wrapper">
  <p class="big-fields-labels">
    Existing Questions:
  </p>
  <% if @multiqs.present? %>
    <% @multiqs.each_with_index do |q, qidx| %>
      <%= qidx + 1 %><%= q.question %>
      <% q.choices.each_with_index do |answer, aidx| %>
        <%= idx + 1 %> <%= answer.name  %>
      <% end %>
      <%= link_to "Remove Question", q, method: "DELETE", data: {confirm: "Are you sure?"} %>
    <% end %>
  <% else %>
    There are no questions yet!
  <% end %>
</div>
<hr class="bold">

